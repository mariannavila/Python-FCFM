#Problema de miltiples mochilas
# Librerias

from itertools import takewhile

val=[]
pes=[]
mt=[]

mt = int(input(print ('Numero de mochilas:' )))

for mt > 0 
  moc[mt]= int(input('Peso permitido en la mochila ' ))
  mt= mt- 1 
  
it = int(input(print ('Numero de items:' )))

for it > 0 
  val[it]= int(input('Valor de item ' ))
  pes[it]= int(input('Peso de item ' ))
  
  it= it- 1 

for mt in range(len(moc)): 
    print moc[mt]


for it in range(len(val)): 
    print val[it]

for it in range(len(pes)): 
    print pes[it]


 






